{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run build && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "lerna": "^7.3.0",
    "typescript": "^5.2.2"
  }
}
