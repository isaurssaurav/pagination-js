{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "publish": "lerna run build && lerna publish --no-private",
    "test": "lerna run test",
    "lint:fix": "prettier . --write",
    "version": "lerna run version"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.8.2",
    "jest": "^29.7.0",
    "lerna": "^7.3.0",
    "prettier": "3.0.3",
    "typescript": "^5.2.2"
  }
}
